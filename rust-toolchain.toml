# Single source of truth for the Rust toolchain.
# Consumed by the nix flake (rust-overlay) and by rustup directly.
[toolchain]
channel = "stable"
profile = "minimal"
components = ["rustfmt", "clippy", "rust-analyzer", "rust-src"]
